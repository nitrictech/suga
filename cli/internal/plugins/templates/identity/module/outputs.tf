output "suga" {
  value = {
    exports = {
      # How services that know how to consume identity resources can find them
      # As an example see:
      # AWS IAM Role:
      # and AWS Lambda: 
    }
  }
}
