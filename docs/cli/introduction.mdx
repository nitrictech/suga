---
title: "Introduction"
description: "Get started with the Suga CLI"
---

## Overview

The Suga CLI is a powerful tool for building, developing, and managing cloud-native applications with the Suga platform.

<Card title="Install Suga CLI" icon="download" href="/cli/installation" horizontal>
  Install the Suga CLI on your system to get started.
</Card>

## Quick Start

```bash
# Install the CLI
brew install nitrictech/tap/suga

# Create a new project
suga new my-app

# Start development
cd my-app
suga dev
```

## Commands Overview

| Command             | Description                                   |
| ------------------- | --------------------------------------------- |
| `suga access-token` | Get access token                              |
| `suga build`        | Builds the Suga application                   |
| `suga completion`   | Generate shell autocompletion                |
| `suga config`       | Manage CLI configuration                      |
| `suga dev`          | Run the Suga application in development mode |
| `suga edit`         | Edit the Suga application                     |
| `suga generate`     | Generate client libraries for Suga           |
| `suga init`         | Setup a new Suga project                      |
| `suga login`        | Login to Suga                                 |
| `suga logout`       | Logout from Suga                              |
| `suga new`          | Create a new Suga project                     |
| `suga team`         | Switch between teams                          |
| `suga templates`    | List available templates                      |
| `suga version`      | Print the Suga CLI version                    |

## Everyday Commands

<CardGroup cols={2}>
  <Card title="Create Projects" icon="folder" href="/cli/new">
    Use `suga new` to create projects from templates or `suga init` to add Suga to existing apps
  </Card>
  <Card title="Visual Editor" icon="pen-to-square" href="/cli/edit">
    Use `suga edit` to design your application architecture and configure deployment targets
  </Card>
  <Card title="Local Development" icon="play" href="/cli/dev">
    Use `suga dev` to run your application locally with hot reload and instant feedback
  </Card>
  <Card title="Generate Clients" icon="code" href="/cli/generate">
    Use `suga generate` to create type-safe client libraries for your APIs
  </Card>
  <Card title="Build Terraform" icon="hammer" href="/cli/build">
    Use `suga build` to generate Terraform infrastructure code for your deployment target
  </Card>
  <Card title="Team Authentication" icon="key" href="/cli/login">
    Use `suga login` to connect to Suga Platform
  </Card>
</CardGroup>

## Global Options

- `-h, --help` - Show help for any command

Use `suga version` to display the CLI version.

## Getting Help

```bash
# Help for any command
suga [command] --help

# General help
suga help
```
